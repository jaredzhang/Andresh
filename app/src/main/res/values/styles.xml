<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/Widget.ActionBar</item>
    </style>

    <style name="AppTheme.Logo">
        <item name="android:actionBarStyle">@style/Widget.ActionBar.Logo</item>
    </style>

    <style name="Widget.ActionBar" parent="android:Widget.Holo.Light.ActionBar">
        <!--...-->
    </style>

    <style name="Widget.ActionBar.Logo">
        <item name="android:logo">@drawable/wheels</item>
        <item name="android:displayOptions">useLogo|showHome</item>
    </style>

    <style name="TextAppearance.Autocomplete.Suggestion"
           parent="android:style/TextAppearance.Holo.Medium">
        <item name="android:fontFamily">sans-serif-condensed</item>
        <item name="android:textColor">@color/autocomplete_suggestion</item>
        <item name="android:textStyle">italic</item>
    </style>﻿
    <color name="autocomplete_suggestion">#DEDEDE</color>

</resources>
